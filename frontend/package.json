{
  "name": "atlas-de-datos-frontend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Visor del Atlas de Datos: lee AGF + perfil de visualización. Sin backend; los documentos no salen del equipo.",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "dev": "npm run sync-data && vite",
    "build": "npm run sync-data && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
